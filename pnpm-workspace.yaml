packages:
  - 'apps/*'
  - 'packages/*'

# Scripts de build de dependencias permitidos (pnpm los bloquea por defecto).
# esbuild compila su binario nativo en postinstall; Vite lo requiere.
allowBuilds:
  esbuild: true
