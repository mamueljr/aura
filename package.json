{
  "name": "aura",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo del ecosistema Aura — apps y paquetes compartidos.",
  "packageManager": "pnpm@11.17.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "preview": "turbo run preview"
  },
  "devDependencies": {
    "turbo": "^2.10.6"
  }
}
